train: data/images/train
val: data/images/test

nc: 7  # número de classes
names: ["W", "U", "B", "R", "G", "C", "M"]  # siglas usadas no script de normalização

obb: true  # uso de caixas rotacionadas (Oriented Bounding Boxes)
